Please create a simple program with python, FastAPI and  confluent-kafka client that can act as a kafka broker/receiver.

The source system will send messages in cot.xml format. These need to be stored into some lightweight solution. 

In addition the received data must be shown in browser as a list (perhaps only main fields selected for display). When one click entry the full message should be displayed in the gui.

Desin also a way to display the cot.xml messages on a OSM (open street map), with coordinates picked from the cot.xml message.